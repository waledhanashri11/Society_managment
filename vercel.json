{
  "buildCommand": "cd Website/frontend && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "Website/frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
