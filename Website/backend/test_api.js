const { promisePool } = require('./database/client'); const ctrl = require('./controllers/maintenanceController'); const req = { headers: { host: 'localhost:3000' }, protocol: 'http' }; const res = { send: (data) => console.log('SEND called'), json: (data) => console.log(JSON.stringify(data, null, 2)), status: (code) => { console.log('Status', code); return res; } }; async function run() { await ctrl.getPendingVerificationPayments(req, res); process.exit(); } run();
